pipeline {
   agent any
   
   stages {
             stage('Git Clone'){
                      steps {
                         echo "git clone url"
                        }
              stage('Maven Build'){
                      steps {
                         echo "mvn clean package"
                        }
 }
  }
   }
}
